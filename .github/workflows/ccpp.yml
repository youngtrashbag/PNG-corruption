name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    - name: mkdir bin
      run: mkdir bin
    - name: cd bin
      run: cd bin
    - name: cmake ..
      run: cmake ..
    - name: make
      run: make
